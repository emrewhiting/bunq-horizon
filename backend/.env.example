# copy to .env and fill in your own keys

# anthropic key for the vision classifier
# https://console.anthropic.com
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6

# bunq sandbox key — optional. without this /balance falls back to bunq_data.json
BUNQ_API_KEY=

PORT=8000
